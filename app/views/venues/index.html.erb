<%= link_to "add a venue", new_venue_path %>

<%= form_tag venues_path, :method => :get do %>
	<p>
		<%= text_field_tag :search, params[:search] %>
		<%= submit_tag "Search Near", :name => nil %>
	</p>
<% end %>


<ul>
	<% @venues.each do |v| %>

		<li data-lat="<%= v.latitude %>" data-lng="<%= v.longitude %>">
			<h3><%= v.title %></h3>
			<p><%= v.address %></p>
		</li>

	<% end %>
</ul>

<section id="map"></section>

