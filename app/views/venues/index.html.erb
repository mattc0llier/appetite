<%= link_to "add a venue", new_venue_path %>

<ul>
	<% @venues.each do |v| %>

		<li data-lat="<%= v.latitude %>" data-lng="<%= v.longitude %>">
			<h3><%= v.title %></h3>
			<p><%= v.address %></p>
		</li>

	<% end %>
</ul>

<section id="map"></section>

